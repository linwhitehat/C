/*keywords*/
array:数组
conversion specification:转换说明符
character string:字符串
null character:空字符
blank:空白字符空格
tab:制表符
newline:换行符
compiletime substitution:编译时代入法
manifest constant:明显常量
enum:枚举
control-string:控制字符串


/*章节记录*/
1.
字符串用双引号表示内容，字符串以空字符\0作为结束标志，使用数组进行存储。

2.
scanf读取内容遇到第一个空白字符空格、制表符、换行符会停止读取。

3.
sizeof以字节为单位给出数据大小
strlen以字符为单位给出字符串长度

strlen记录字符串可见字符作为字符串长度，sizeof记录对应变量占据空间大小，对于固定开辟空间的变量，sizeof返回其空间大小，结果是固定的，而strlen返回实际字符的长度，结果是变化的。

4.
